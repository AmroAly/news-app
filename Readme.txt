# Steps to create and initialize the database
1. create a database and call it news.
2. if you have a password for your mysql Please add it to .env DB_PASSWORD and also don't forget to add the DB_USERNAME as well.
3. then run `php artisan migrate --seed`

# Up and Running
To run the app just run `php artisan serve` and go to `localhost:8000` in your browser.
